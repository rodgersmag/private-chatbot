# PostgreSQL Configuration
POSTGRES_DB=selfdb # <-- Change me when in prod
POSTGRES_USER=selfdb_user # <-- Change me when in prod
POSTGRES_PASSWORD=selfdb_password # <-- Change me when in prod
DATABASE_URL=postgresql+asyncpg://selfdb_user:selfdb_password@postgres:5432/selfdb #update me to match the values you entered above

# Storage Service Configuration
STORAGE_SERVICE_URL=http://storage_service:8001
STORAGE_SERVICE_EXTERNAL_URL=http://localhost:8001 # <--- Set a publically accessible URL here to use storage. 
STORAGE_PATH=/data/storage

# Backend API / JWT Configuration
SECRET_KEY=changeme_super_secret_jwt_key_32_bytes_long # <-- Will be auto-generated by start.sh
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
API_PORT=8000

# Frontend Configuration
FRONTEND_PORT=3000

# For local development (browser to backend)
REACT_APP_API_URL=/api/v1 

# Default Admin Credentials
DEFAULT_ADMIN_EMAIL=admin@example.com  # <-- Change me when in prod
DEFAULT_ADMIN_PASSWORD=adminpassword   # <-- Change me when in prod

# Cloud Functions Configuration
FUNCTIONS_DIR=/functions

# Anonymous API Key for public access
ANON_KEY= # <-- Will be auto-generated by start.sh

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://localhost:5173,http://localhost:3000 # Comma-separated list of allowed origins

# Storage Service API Key
STORAGE_SERVICE_API_KEY= # <-- Will be auto-generated by start.sh

# Optional: Email for Password Reset (configure if implementing email sending)
# MAIL_USERNAME=
# MAIL_PASSWORD=
# MAIL_FROM=your_email@example.com
# MAIL_PORT=587
# MAIL_SERVER=smtp.example.com
# MAIL_STARTTLS=True
# MAIL_SSL_TLS=False